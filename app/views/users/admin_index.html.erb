<h2>Users</h2>

検索
<%= form_tag admin_user_index_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :last_name => nil, :first_name => nil %>
  </p>
<% end %>

<form name="sort_form">
  並び替え
    <select name="sort" onchange="dropsort()">
        <option value="/admin/users"> すべて </option>
        <option value="/admin/users?sort=id desc"<% if @navi == 'id desc' %>selected <% end %>>新しい順番</option>
        <option value="/admin/users?sort=id asc"<% if @navi == 'id asc' %>selected <% end %>>古い順番</option>
        <option value="/admin/users?sort=last_name_kana asc"<% if @navi == 'last_name asc' %>selected <% end %>>50音順</option>
    </select>
</form>
<h4 class="total">総ユーザー数 <%= @total %> 人</h4>

<table class='table'>
  <thead>
    <tr>
      <th>ID</th>
      <th>登録日時</th>
      <th>氏名</th>
      <th>欲</th>
      <th></th>
    </tr>
  </thead>

  <% @users.each do |user| %>
  <tbody class="<% if user.deleted_at.present? %>unsubscribe<% end %>">
    <tr>
      <td><%= user.id %></td>
      <td class="create"><%= user.created_at.to_s(:db) %></td>
      <td><%= link_to admin_user_path(user.id) do %><%= user.last_name %> <%= user.first_name %><% end %></td>
      <td><%= attachment_image_tag user.greed, :greed_image_name, :fill, 30, 30, format: 'jpeg' %></td>
      <td><%= link_to "Edit", edit_admin_user_path(user.id), class: "btn-sm btn-success" %></td>
    </tr>
  </tbody>
  <% end %>
</table>
<% if @users.empty? %> 該当するユーザーは、見つかりませんでした。<% end %>
<%= paginate @users %>
<%= @users.total_count %>件中<%= @users.offset_value %>〜<%= @users.offset_value + @users.length %>件を表示しています。

